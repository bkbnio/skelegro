group = "org.leafygreens"
version = "0.0.1"

plugins {
  kotlin("jvm") version "1.4.21"
  kotlin("kapt") version "1.4.21"
  id("com.github.johnrengelman.shadow") version "6.0.0"
  application
  maven
}

application {
  @Suppress("DEPRECATION")
  mainClassName = "org.leafygreens.backbone.engine.MainKt"
}

repositories {
  jcenter()
  maven("https://jitpack.io")
  github("https://maven.pkg.github.com/rgbrizzlehizzle/blue-whale")
  mavenLocal()
}

dependencies {
  implementation(kotlin("stdlib"))
  implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.4.2")
  implementation(platform("software.amazon.awssdk:bom:2.14.9"))
  implementation("software.amazon.awssdk:s3")
  testImplementation("org.junit.jupiter:junit-jupiter:5.6.2")
}

tasks {
  build {
    dependsOn("shadowJar")
  }
  withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile::class).all {
    kotlinOptions {
      jvmTarget = "14"
    }
  }
  named<com.github.jengelman.gradle.plugins.shadow.tasks.ShadowJar>("shadowJar") {
    @Suppress("DEPRECATION")
    classifier = null
  }
}
